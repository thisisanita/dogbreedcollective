import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#825514",
    },
    secondary: {
      main: "#715A41",
    },
  },
});

export default theme;
